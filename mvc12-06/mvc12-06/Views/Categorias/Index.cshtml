@model IEnumerable <Mvc1206.Models.Categoria>
@{
    ViewBag.Title = "Categorias";
}

@*
    <h2>@ViewBag.Title</h2>

    @foreach(string cat in ViewBag.ListaCategorias)
    {
        <p>@cat</p>
    }
*@

@foreach (var cat in Model)
{
    <b>@cat.Nome</b>
    <p>@cat.Descricao</p>
}